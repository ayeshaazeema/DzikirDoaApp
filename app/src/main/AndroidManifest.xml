<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ayeshaazeema.dzikirdoaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DzikirDoaApp">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".activity.DzikirPetangActivity" />
        <activity android:name=".activity.DzikirPagiActivity" />
        <activity android:name=".activity.DzikirPagiPetangActivity" />
        <activity android:name=".activity.DzikirSetiapSaatActivity" />
        <activity android:name=".activity.QauliyahActivity" />
        <activity android:name=".activity.DzikirDoaHarianActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>